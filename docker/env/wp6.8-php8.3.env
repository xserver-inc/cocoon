# WordPress 6.8 / PHP 8.3（安定版・既定）
# 起動: docker compose --env-file env/wp6.8-php8.3.env up -d
#       （この組み合わせは docker compose up -d の既定値と同じ）
COMPOSE_PROJECT_NAME=cocoon-wp68-php83
WP_IMAGE=wordpress:6.8-php8.3-apache
WEB_PORT=8085
DB_PORT=3085
PMA_PORT=8185
